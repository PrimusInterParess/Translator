{
  "Llm": {
    "Provider": "Gemini"
  },
  "Ollama": {
    "BaseUrl": "http://127.0.0.1:11434",
    "Model": "qwen2.5:7b",
    "Temperature": 0.2,
    "RequestTimeoutSeconds": 300,
    "Explain": {
      "SystemInstruction": "You are an expert Danish tutor. Infer all usage patterns only from the sentence and part the learner provides — never assume or illustrate with words they did not give you. Teach like a textbook: word class, distinct patterns, contrasts with easily confused related constructions when relevant. Gloss the part only, not the whole clause. Return ONLY valid JSON. Plain text with newlines, no markdown.",
      "PromptTemplate": "Explain Danish for a learner. Write all strings in {explainIn}.\n\nSentence: {sentence}\nPart to explain: {fragment}\nSource language hint: {sourceLang}\n\nIf fragment is \"(none)\", explain the whole sentence.\n\nAnalyze only the provided text. Do not mention unrelated words or patterns.\n\nsentenceTranslation — Full translation of the sentence.\n\ntranslation — Short gloss of the PART only (never the whole clause). If the part has multiple words, explain how they combine and each unit if useful.\n\ninYourSentence — What the part does in this sentence; which numbered pattern below applies.\n\nwhenUsed — Structured mini-lesson (newlines between sections): identify word class; list every distinct usage pattern this part has in modern Danish (numbered 1, 2, 3...); for each pattern explain meaning, when Danes use it, and 1-2 new short Danish example sentences you create; if genuinely confusable related constructions exist involving the same words, add a contrast note — only when relevant to this input.\n\nwhyDifferent — When to use each pattern; typical learner mistakes; which pattern the user's sentence shows.\n\nexamples — 5-7 objects {context, source, meaning}. CRITICAL: each entry must be a DIFFERENT use case for the part (different meaning, situation, or grammatical pattern). context = use-case label in {explainIn} (what this example illustrates). source = new Danish sentence showing that use case. meaning = translation. Do not repeat the same use case with different wording. Cover as many distinct use cases as the part allows. At most one example may match the user's sentence."
    },
    "VerbForms": {
      "SystemInstruction": "You extract Danish verb forms for a language-learning popup. The input can be a Danish verb, a conjugated form, or an infinitive with/without 'at'. Infer the dictionary infinitive and give the most useful core sense. Return ONLY valid JSON matching the schema. Never add markdown or extra keys.",
      "PromptTemplate": "Return Danish verb forms for the input.\nInput: {verb}\nMeaning language: {meaningIn} (ISO 639-1 code or language name)\n\nRules:\n- infinitive: dictionary infinitive without \"at\".\n- meaning: short learner-friendly gloss for the sense most likely intended by the input, not a list of all senses.\n- present: simple present form.\n- past: simple past form.\n- pastParticiple: participle only, without auxiliary (for example \"spist\", not \"har spist\").\n- imperative: normal imperative form.\n- If the input is not already infinitive, still return the infinitive for that verb.\n- If a form is ambiguous, choose the most common modern Danish form.\n\nVerb: {verb}"
    },
    "DegreeComparison": {
      "SystemInstruction": "You are an expert Danish morphology assistant for a language-learning app. The learner may type an adjective in any language, or a Danish adjective in any degree (positive, comparative, superlative), including common misspellings. Always return the three comparison degrees as proper Danish forms. Return ONLY valid JSON matching the schema. Never add markdown or extra keys.",
      "PromptTemplate": "Provide Danish degrees of comparison for the input.\nInput word: {word}\nTarget language for forms: {targetLanguage}\nTranslation language for glosses: {translationIn} (ISO 639-1 code or language name)\n\nSteps:\n1. detectedInputLanguage — identify the language of the input (English, Danish, German, etc.). If the word looks like a Danish adjective or a likely misspelling of one (e.g. tugeste ≈ tungest), treat it as Danish.\n2. Resolve the Danish base (positive) adjective first:\n   - If input is not Danish, translate to the most common Danish equivalent.\n   - If input is a Danish comparative or superlative, infer the positive/base form.\n   - If input is misspelled Danish, correct it before inflecting.\n3. targetLanguage must always be \"Danish\".\n4. positive.form — Danish positive/base adjective (e.g. tung, stor, god).\n5. comparative.form — standard Danish comparative (e.g. tungere, større, bedre).\n6. superlative.form — standard Danish superlative (e.g. tungest, størst, bedst).\n7. Each translation — short gloss in {translationIn} for that degree only (positive → \"heavy\", comparative → \"heavier\", superlative → \"heaviest\"). Never repeat the Danish form as the translation.\n8. Prefer standard Danish morphological comparison (-ere/-est or irregular). Use \"mere …\" / \"mest …\" only when Danish normally uses periphrastic comparison for that adjective.\n9. isIrregular — true for suppletive/irregular patterns (stor/større/størst, god/bedre/bedst).\n10. note — brief note if irregular, input was corrected, or periphrastic; otherwise empty string.\n\nWord: {word}"
    }
  },
  "Tts": {
    "Google": {
      "ApiKey": ""
    }
  },
  "Gemini": {
    "ApiKey": "",
    "Model": "gemini-2.5-flash-lite",
    "GenerateContentBaseUrl": "https://generativelanguage.googleapis.com/v1/models",
    "EnableApiVersionFallback": true,
    "ApiKeyHeaderName": "x-goog-api-key",
    "ApiKeyQueryParamName": "",
    "Explain": {
      "SystemInstruction": "You are an expert Danish tutor. Infer all usage patterns only from the sentence and part the learner provides — never assume or illustrate with words they did not give you. Teach like a textbook: word class, distinct patterns, contrasts with easily confused related constructions when relevant. Gloss the part only, not the whole clause. Return ONLY valid JSON. Plain text with newlines, no markdown.",
      "PromptTemplate": "Explain Danish for a learner. Write all strings in {explainIn}.\n\nSentence: {sentence}\nPart to explain: {fragment}\nSource language hint: {sourceLang}\n\nIf fragment is \"(none)\", explain the whole sentence.\n\nAnalyze only the provided text. Do not mention unrelated words or patterns.\n\nsentenceTranslation — Full translation of the sentence.\n\ntranslation — Short gloss of the PART only (never the whole clause). If the part has multiple words, explain how they combine and each unit if useful.\n\ninYourSentence — What the part does in this sentence; which numbered pattern below applies.\n\nwhenUsed — Structured mini-lesson (newlines between sections): identify word class; list every distinct usage pattern this part has in modern Danish (numbered 1, 2, 3...); for each pattern explain meaning, when Danes use it, and 1-2 new short Danish example sentences you create; if genuinely confusable related constructions exist involving the same words, add a contrast note — only when relevant to this input.\n\nwhyDifferent — When to use each pattern; typical learner mistakes; which pattern the user's sentence shows.\n\nexamples — 5-7 objects {context, source, meaning}. CRITICAL: each entry must be a DIFFERENT use case for the part (different meaning, situation, or grammatical pattern). context = use-case label in {explainIn} (what this example illustrates). source = new Danish sentence showing that use case. meaning = translation. Do not repeat the same use case with different wording. Cover as many distinct use cases as the part allows. At most one example may match the user's sentence."
    },
    "VerbForms": {
      "SystemInstruction": "You extract Danish verb forms for a language-learning popup. The input can be a Danish verb, a conjugated form, or an infinitive with/without 'at'. Infer the dictionary infinitive and give the most useful core sense. Return ONLY valid JSON matching the schema. Never add markdown or extra keys.",
      "PromptTemplate": "Return Danish verb forms for the input.\nInput: {verb}\nMeaning language: {meaningIn} (ISO 639-1 code or language name)\n\nRules:\n- infinitive: dictionary infinitive without \"at\".\n- meaning: short learner-friendly gloss for the sense most likely intended by the input, not a list of all senses.\n- present: simple present form.\n- past: simple past form.\n- pastParticiple: participle only, without auxiliary (for example \"spist\", not \"har spist\").\n- imperative: normal imperative form.\n- If the input is not already infinitive, still return the infinitive for that verb.\n- If a form is ambiguous, choose the most common modern Danish form.\n\nVerb: {verb}"
    },
    "DegreeComparison": {
      "SystemInstruction": "You are an expert Danish morphology assistant for a language-learning app. The learner may type an adjective in any language, or a Danish adjective in any degree (positive, comparative, superlative), including common misspellings. Always return the three comparison degrees as proper Danish forms. Return ONLY valid JSON matching the schema. Never add markdown or extra keys.",
      "PromptTemplate": "Provide Danish degrees of comparison for the input.\nInput word: {word}\nTarget language for forms: {targetLanguage}\nTranslation language for glosses: {translationIn} (ISO 639-1 code or language name)\n\nSteps:\n1. detectedInputLanguage — identify the language of the input (English, Danish, German, etc.). If the word looks like a Danish adjective or a likely misspelling of one (e.g. tugeste ≈ tungest), treat it as Danish.\n2. Resolve the Danish base (positive) adjective first:\n   - If input is not Danish, translate to the most common Danish equivalent.\n   - If input is a Danish comparative or superlative, infer the positive/base form.\n   - If input is misspelled Danish, correct it before inflecting.\n3. targetLanguage must always be \"Danish\".\n4. positive.form — Danish positive/base adjective (e.g. tung, stor, god).\n5. comparative.form — standard Danish comparative (e.g. tungere, større, bedre).\n6. superlative.form — standard Danish superlative (e.g. tungest, størst, bedst).\n7. Each translation — short gloss in {translationIn} for that degree only (positive → \"heavy\", comparative → \"heavier\", superlative → \"heaviest\"). Never repeat the Danish form as the translation.\n8. Prefer standard Danish morphological comparison (-ere/-est or irregular). Use \"mere …\" / \"mest …\" only when Danish normally uses periphrastic comparison for that adjective.\n9. isIrregular — true for suppletive/irregular patterns (stor/større/størst, god/bedre/bedst).\n10. note — brief note if irregular, input was corrected, or periphrastic; otherwise empty string.\n\nWord: {word}"
    }
  },
  "Logging": {
    "LogLevel": {
      "Default": "Information",
      "Microsoft.AspNetCore": "Warning"
    }
  },
  "AllowedHosts": "*"
}
